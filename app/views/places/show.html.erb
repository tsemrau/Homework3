<h1><%= @place.name %></h1>

<% for post in @place.posts %>
    <h2><%=post.title%></h2>
    <p><%= post.posted_on %></p>
    <p><%= post.description%></p>
    <br>
<% end %>

<a href = "/posts/new?place_id=<%= @place.id %>">New Post</a>